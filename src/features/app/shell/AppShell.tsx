/* Placeholder - to be replaced by the ported app shell from App v2.dc.html. */
import { Outlet } from 'react-router-dom'

export function AppShell() {
  return (
    <div className="surface-app">
      <Outlet />
    </div>
  )
}
