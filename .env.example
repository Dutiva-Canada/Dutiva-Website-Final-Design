# HR Documents Library — Supabase read path (public.doclib_* views, RLS read-only).
# Both values are publishable by design (the anon key only reaches read-only demo
# views). Without them the app serves the identical bundled fixtures.
VITE_SUPABASE_URL=https://khtwpxnvziiyplaflwru.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_4cpptaftDcwRSI0K2KuBjA_0vJcC0-S
